# 観劇・イベント・飲食ハブ 設計書

## 1. 全体像

3ドメイン（観劇・イベント・飲食店）を単一のデータハブに集約し、静的サイトとして公開、週次でClaudeが推薦を生成する構成です。

```
[データソース] → [Notion（ハブ）] → [GitHub Actions（週次）] → [静的サイト + 週次ダイジェスト]
                                          ↑ Claude API（推薦生成）
```

## 2. ドメイン別の要件とデータ取得可否

| ドメイン | ソース | 取得方法 | 難易度 |
|---|---|---|---|
| 観劇 | 松田観劇カレンダー（公開Googleカレンダー） | iCalフィード or Calendar API。既存ID流用可 | 低 |
| イベント全般 | 現状は散在（Notion、React製トラッカー等） | Notion DBを正とする。登録フローはMCP経由で既存の観劇登録と同型化 | 中 |
| 飲食店 | ローカルガイド投稿 | 注意点あり（下記） | 高 |

飲食店の制約: Googleに「自分の投稿」を取得する公式APIがありません。現実解は2つの併用です。
1. 過去分: Google Takeoutで一括エクスポートし、初回のみNotionへ取り込み
2. 今後分: 現在のレビュー下書きワークフロー（Claudeで210〜230字生成）の完了時に、同じセッションでNotion DBへも保存する運用に変更。これで二重入力なしに蓄積されます

## 3. データモデル（Notion DB 3本）

共通プロパティ: タイトル / 日付（期間）/ エリア / URL / タグ / ステータス（気になる・予定・済）/ 評価 / メモ

- 観劇DB: 劇団・会場・ジャンル（小劇場/ダンス/舞踏 等）を追加
- イベントDB: 種別（展示/フェス/トーク 等）を追加
- 飲食DB: ジャンル・価格帯・再訪意向・レビュー本文を追加

観劇はGoogleカレンダーが正のままでよく、Notionへは同期のみ（カレンダー運用のv4プロトコルは変更不要）。

## 4. サイト設計

- ホスティング: GitHub Pages（無料・Claude Codeとの相性が良い）
- 生成: GitHub ActionsがNotion API + カレンダーフィードからJSONを生成し、静的ビルド
- 画面: ①統合カレンダービュー ②ドメイン別リスト（フィルタ付き）③週次ダイジェストページ ④飲食マップビュー
- 更新頻度: 日次ビルドで十分（イベント情報の鮮度要件は高くない）

## 5. 推薦機構（週次）

GitHub Actionsで毎週金曜などに実行:

1. 入力: 直近登録データ + 未消化の「気になる」+ 嗜好プロファイル
2. 嗜好プロファイルはリポジトリ内のJSONで明示管理（小劇場・コンテンポラリー・舞踏への傾向、フォロー中の劇団/振付家、トマト・玉ねぎ除外、生活圏=代々木上原/渋谷、月間観劇ペース 等）。ブラックボックス化せず自分で編集できるのが利点
3. Claude APIが「今週の推薦5件 + 理由 + 空き日程との突合」を生成
4. 出力先: サイトのダイジェストページ + Gmail送信（MCPまたはAPI）。Instagram Stories用素材への転用も既存パイプラインに接続可能

## 6. 着手順序

| フェーズ | 内容 | 目安 |
|---|---|---|
| P1 | 観劇のみでサイト公開（カレンダーフィード→静的サイト） | 1〜2セッション |
| P2 | Notion DB 3本設計 + 飲食のTakeout取り込み + レビュー保存フロー変更 | 2〜3セッション |
| P3 | 週次推薦バッチ + メール配信 | 1〜2セッション |
| P4 | イベント全般の登録運用定着、マップビュー | 随時 |

P1が最速で価値が出る。観劇カレンダーは既に公開・構造化済みなので、フィードURLがあればP1に着手できる。

## 公開範囲の方針（確定 2026-07-06）

- サイト・リポジトリとも完全公開（GitHub Pages無料プランはpublicリポジトリ必須のため）
- 個人情報はリポジトリに置かない。嗜好プロファイルのうち生活圏・食の除外項目など私的な部分は、GitHub ActionsのSecretsまたはNotion側で管理する
- 観劇の過去アーカイブもサイトに表示する（年別・ジャンル別で閲覧可能に）
- 飲食店データはDB側に「公開/非公開」フラグを持たせ、後から切り替え可能な設計とする

## 週次推薦の実行基盤（確定 2026-07-06）

- GitHub Actions + Anthropic APIキー（従量課金）ではなく、Claude Codeのscheduled task（サブスク利用枠、追加課金なし）で週次実行する
- 実行結果はリポジトリにcommitしてサイトのダイジェストページに反映する
- 推薦ロジック自体は単体スクリプトとして切り出し、基盤を後から変更できるようにしておく

## 未確定事項

解決済み（2026-07-07）:
- [x] iCalフィードURL: `https://calendar.google.com/calendar/ical/ooc2a3r5b532knt1472khnrjpg%40group.calendar.google.com/public/basic.ics`（取得確認済み、872件）
- [x] カレンダーID・タイムゾーン: `ooc2a3r5b532knt1472khnrjpg@group.calendar.google.com` / Asia/Tokyo
- [x] ステータス表現: カレンダーには「見込まれる公演」のみ登録されており、確定/済の区別はない。ステータス管理はP2でNotion側に持たせる
- [x] Notion InboxページID: `393020b1f16781fbab1bf6ec16b8a3b1`

未解決:
- [ ] 週次ダイジェストのGmail送信方式（P3。scheduled taskからGmail MCPが使えるか要検証、代替はSMTP）
- [ ] 推薦の「空き日程との突合」に使う個人カレンダーへのアクセス方法（P3。秘密iCalアドレス等）

## P1実装状況（2026-07-07）

実装・ローカル動作確認済み:
- scripts/build_data.py: フィード取得→site/data/events.json 生成（Python 3.9 stdlibのみ）。872件全件パース、URL抽出681件、劇団/作品分離652件
- site/: 単一ページ（開催中・今後 / 過去アーカイブ年別 / 検索）。フレームワーク・CDN依存なし、ダークモード・モバイル対応
- .github/workflows/build.yml: 日次（JST 6:00）+ push + 手動のPagesデプロイ。公開後に稼働

## 公開完了（2026-07-12）

- リポジトリ: https://github.com/tytydy-cyber/kangeki-hub （public）
- 公開URL: https://tytydy-cyber.github.io/kangeki-hub/
- git著者はGitHub noreplyアドレス（`115096152+tytydy-cyber@users.noreply.github.com`）に統一済み（全コミット書き換え済み、実メール・ホスト名は非公開）
- `.github/workflows/build.yml` が日次（JST 6:00）でフィードを再取得・再デプロイ
- 本番URLで開催中・今後・過去アーカイブ・検索の動作を確認済み

P1完了。次はP2（Notion DB設計・飲食データ取り込み）。

## P1.5: サイト機能拡張 + ダイジェスト初回（2026-07-12）

- 月別まとめ: 「今後」を月見出しで区切り、アーカイブを年→月階層に
- 地図: 本格マップは見送り。会場名をGoogleマップ検索リンクに（LOCATIONの住所を利用）
- 劇団タグ: カード上の劇団チップ + 「劇団別」タブ（公演数順の一覧→タップで劇団ページ）
- 週次ダイジェスト: scripts/analyze_trends.py で傾向集計し、Web調査と合わせて site/data/digest.json + digest.html に掲載。初回は手動生成（手順はCLAUDE.md）。定期化（scheduled task）は内容確認後に判断

## P1.6: 日程逆引き + ダイジェスト/提案の分離（2026-07-15）

- 日程逆引き: カレンダーに期間指定欄を追加。指定期間と重なる公演を過去・未来問わず横断表示（片方だけの指定は単日）
- ダイジェストを「傾向ダイジェスト」に再定義: 年次など広範囲の分析専用（年別件数・開催月分布・上位劇団/会場）。提案は分離
- 提案を「おすすめ提案」ページ（proposals.html）に独立: 直近約1ヶ月の未登録公演 + 期間問わずの特筆枠
- analyze_trends.py に年別・開催月ヒストグラムを追加
- 既知課題: 会場名の英語表記混入（build_data.pyでの正規化が今後の改善点）

## P1.7: 会場名正規化 + 提案の週次自動化（2026-07-15）

- 会場名正規化: build_data.py に VENUE_ALIASES を追加し、Googleカレンダーの英語ローカライズ会場名（例: Hanazono Shrine→花園神社）を日本語の正式表記へ統一。集計・表示のみ正規化し、マップリンクに使う location（住所）は温存するため紐付けは維持
- 提案の週次自動化: scheduled task `kangeki-weekly-proposals`（毎週金曜0時JST）を設定。手順書 docs/WEEKLY_TASK.md に従い、proposals.json / digest.json の再生成と会場名マッピングの保守を自動で行う

## P1.8: 劇団名プレフィックス除去 + 劇団概要（2026-07-15）

- 劇団名プレフィックス除去: build_data.py の split_title で先頭の注記プレフィックス（（野外）（京都）（映像）（無料）（中止）等）を除去し、company を統合（例: （名古屋）優しい劇団→優しい劇団）。title は温存
- 劇団概要: site/data/companies.json を新設し、劇団詳細ページに概要ブロックを表示（app.js の overviewBlock）。概要が無い劇団は非表示で degrade。初回は主要5団体をシード、以降は週次タスクが毎週数件ずつ Web調査で拡充

## P1.9: 劇団名の表記揺れ正規化（2026-07-15）

- build_data.py に COMPANY_ALIASES を追加。プレフィックス除去後、略称・「劇団」有無・別公演名などの表記揺れをメジャー表記へ統合（例: ゴキコン→ゴキブリコンビナート、不労社→劇団不労社、唐組若手公演→唐組、劇団唐ゼミ/唐ゼミ→劇団唐ゼミ☆）
- 同一劇団と確実に言えるものだけ統合。コラボ「A×B」・大学の別企画・別劇団は対象外
- 週次タスクの保守対象に劇団名の表記揺れも追加（会場名と同じ枠組み）。ユニーク劇団数 354→337

## P1.10: 公演単位の集計 + ページ概要文（2026-07-15）

- 公演単位の集計: build_data.py で各イベントに production ID を付与（同一劇団・同一作品を日程近接でクラスタ化、gap 60日超は再演として別公演）。劇団別の件数・ダイジェストの上位劇団を「公演数」で数えるよう変更（例: 唐組 25登録→8公演、優しい劇団 20→12）。劇団詳細も公演単位でまとめて表示し件数を一致
- トップのページ概要文を、実態（小劇場・アングラ中心、見込み公演を公開カレンダーから自動更新）に合わせた2〜3行の説明に差し替え

## P1.11: PWA化（オフライン対応）（2026-08-01）

- manifest.json + Service Worker + アイコンで、インストール可能・オフライン対応のPWAに（Android前提）
- SW: アプリシェルはprecache+network-first、data/*.jsonはstale-while-revalidate。インストール時に全ページ＋全データをprecacheし初回からオフライン可。scope相対URLでルート/サブパス両対応
- アイコンは scripts/gen_icons.py（標準ライブラリのみ）で生成した紅テントモチーフ。maskable対応
- 本番（/kangeki-hub/）でSW登録・キャッシュ・オフライン表示を確認済み
- 次段（未対応）: プッシュ通知や多人数対応はバックエンドが必要。データ権利・パイプラインのクラウド化が配信プロダクト化の主なハードル

## P1.12: UI 操作性・リッチ化パス（2026-08-01）

- 情報密度の改善: 日付＋月ボタンを「日程で絞り込む」の開閉式（details）に畳み、既定で本文にすぐ届くように
- ナビ追従: タブ＋検索を sticky-bar 化（スクロールで影が付く）。タブはピル型・折り返し解消（過去アーカイブ→過去）
- カードの開催状態を色分け: 開催中＝アクセント帯＋塗りバッジ、7日以内開幕＝アンバー帯＋バッジ、それ以降＝無彩色。cardStatus() で判定
- 上へ戻るFAB、検索の結果件数表示、当月ボタンの目印、ホバー/トランジション、focus-visible、prefers-reduced-motion 対応
- ライト/ダーク両テーマ・モバイルで確認済み

## P1.13: 会場別タブ・今週ボタン・テーマ切替・ナビ改善（2026-08-02）

- 会場別タブ: 劇団別と対称構造（venueStats/renderVenues/collapseProductions再利用）。会場詳細では会場リンクを隠し劇団チップを残す（card()にnoVenueオプション追加。劇団詳細はその逆でnoChip）
- 「今週」ワンタップ: 月ボタン列の先頭に追加。月曜始まりの週区切り（thisWeekRange）。トグルで解除可
- 手動テーマ切替: 全ページ右上のボタンで 自動→ライト→ダーク→自動 を循環。localStorage永続化、data-theme属性でCSS変数を上書き（:root[data-theme]がメディアクエリより高詳細度で優先）。FOUC防止のためhead先頭にインラインスクリプトで即時適用
- ナビ改善: 「おすすめ提案」「傾向ダイジェスト」「カレンダー」のリンクをテキストリンクからアイコン付きピルボタン（.page-link）に変更
- sw.js VERSION を v2 に更新（theme.js追加のため）

## P1.14: ライトモード廃止・提案の即時補充運用（2026-08-11）

- ライトモード機能を全廃止。theme.js削除、3ページのトグルボタン・inline anti-FOUCスクリプト・script tag削除、style.cssの `:root[data-theme]` / `@media (prefers-color-scheme: dark)` 分岐を撤去し、ダーク配色を唯一の `:root` に固定。端末のOS設定に関わらず常時ダーク表示。sw.js VERSION を v3 に更新（precacheからtheme.js除去）
- 提案の運用ルール確定: チャットで採用/却下を伝えられたら、その場でカレンダー登録・proposals.jsonからの削除に加え、最低1件程度の代替候補を即座に補充してからcommit/push（次の金曜まで空欄で放置しない）
- 教訓: 唐組「虹屋敷」をspecialに未発表として載せたが実際は登録済みだった事故があったため、special（ウォッチ枠）もnextMonthと同様にevents.jsonとの突き合わせを必須化。また劇団道学先生の提案URLが会場の月間ラインナップ一覧ページで該当公演に辿り着けなかった指摘を受け、提案のurlは公演固有の詳細ページを優先する方針に。いずれもdocs/WEEKLY_TASK.mdと定期タスクのプロンプトに反映済み

## P1.15: 提案精度の向上（会場・劇団ローテーション）（2026-08-21）

「たまたま検索でヒットした所だけ確認する」運用では、頻出会場・頻出劇団を網羅的にチェックできていなかった。広さ（情報源の網羅性）と深さ（マッチング精度）の両面で改善案を出し、以下2点を採用:

- `scripts/rotation_pick.py` を新設。ISO週番号を種に、上位会場3件・上位劇団4件を状態ファイルなしで機械的に週替わり選出（会場は約5週、劇団は約5週で全件を一周）
- **会場ローテーションは「どこを探すか」の探索範囲選定であり、推薦理由にはしない**（会場は劇団と違って本人の嗜好と直結しないとの指摘を受けて明確化）。reasonには作品の中身・作風で書く
- **劇団ローテーションは新規開拓ではなく漏れ検知**: 頻出劇団＝登録頻度が高い＝好きな劇団という前提のもと、その劇団の新作アナウンスを見落としていないかを確認する目的。次回公演が既に登録済みの劇団は軽く確認するだけでよい
- WEEKLY_TASK.md ステップ1・4、定期タスクのプロンプトに反映済み

追加の教訓（2026-08-22）: 渡辺源四郎商店「翔べ！原子力ロボむつ〜めぐりあい時空編」の提案URLが、URLスラッグの語感（"robo.html"）だけで選んだ結果、同劇団の**別作品**（過去公演）のDVD販売ページになっていた。中身を確認せずURLパターンで推測するとこの種の事故が起きるため、「URLは必ずfetchしてタイトル・日程が一致するか確認してから使う」ことをWEEKLY_TASK.mdに明記。なお同作は初演（2012年）が岸田國士戯曲賞候補になっており、この事実の方が「アングラ・小劇場路線」という曖昧な理由より説得力のある推薦理由になる、との指摘も反映（reasonは可能なら受賞歴・戯曲賞候補歴のような具体的事実を優先する）

追加の教訓（2026-09-16）: ザジ・ズー「ザジモン!!」（黄金町バザール2026参加作品）を提案したが、events.jsonには既に「上大岡バザール2026」（9/10〜10/12）が登録済みで、そのnoteに「黄金町バザールと同時開催」と明記されていた。タイトル文字列の突き合わせ（「ザジ」「ザジモン」等）だけでは、実際の登録がフェスティバルの別名・親イベント名でなされているケースを検出できない。今後は候補演目が芸術祭・バザール等の一部である場合、events.jsonの該当イベントのnote（説明文）まで読んで同時開催・関連イベント名が書かれていないか確認すること。WEEKLY_TASK.mdに反映予定。

検討したが見送った案（理由付きで記録。今後のセッションで再提案不要）:
- **採用/却下履歴のデータ化とフィードバック学習**: 運用が面倒という理由で見送り
- **観劇後の評価記録による重み付け**: ユーザーに評価入力の手間を強いるため見送り
- **日程競合チェック・提案のスコア閾値化**: ユーザーの意図に沿わないため見送り（閾値化は特に「空にせず最低1件は出す」という既存の運用ルールと矛盾する）

## P1.16: 情報源の追加とテイスト軸の明文化（2026-09-16）

「ローテーションが空振りして提案が埋まらない」が3回続いたのを受け、ユーザー判断で**情報源を増やす方向**に舵を切った。あわせて「会場の頻出度は精度を担保しない。登録傾向から好きな劇団・作品のテイストを抽出して提案に活かせ」という指示があり、以下を確定:

- **公社流体力学のnote連載「公社が◯月に見たい演劇」を最重要の情報源に追加**。毎月30件前後の公演が日程・会場・講評つきで並ぶ。嗜好の重なりが大きく、2026-10月号の30件のうち8件が既にカレンダー登録済みだった（＝この人の目利きとカレンダーの傾向がほぼ一致している裏付け）。同劇団自体もカレンダー登録歴あり。最新記事は note の公開API `https://note.com/api/v2/creators/kryuutai/contents?kind=note&page=1` から `key` を引く
- **芸術祭・フェスティバルの公式ラインアップを情報源に追加**。1つ確認するだけで候補が大量に取れる。「芸術祭のうち1演目だけ登録されていて残りが漏れている」状態が実際に起きていた（秋の隕石2026東京は13演目中12演目が未登録、ダダルズ1件のみ登録済み）
- **会場ローテーションは優先度を下げた**（廃止はしない）。3回連続で収穫ゼロ。会場は劇団と違って本人の嗜好と直結しないため、探索範囲としても効率が悪い
- **テイスト軸を9分類でWEEKLY_TASK.mdに明文化**（アングラ・テント／舞踏・身体／キワモノ／ポストドラマ／不条理サブカル／現代口語／小空間・一人芝居／回遊型サイトスペシフィック／テクノロジー×身体）。events.jsonの登録履歴から抽出したもので、各軸に登録済みの実例を併記した
- **初見の劇団を恐れない方針を明記**。直近1年の登録245件のうち98団体が「1回だけ」の登録で、広く浅く新規開拓する傾向が明確。頻出劇団の新作を追うだけでは提案が枯れる
- `nextMonth` の目標件数を4〜8件→**6〜10件**、windowを約1ヶ月→**約6週間**に拡大
- reasonは「カレンダーの登録履歴と結びつける」のが最強という方針を明記（例: 花形槙「エルゴノミクス胚」は2025-11登録の「エルゴノミクス胚・プロトセル」の発展形）
- 副産物として `COMPANY_ALIASES` に `花形慎→花形槙` を追加（登録時の誤字。作品名「エルゴノミクス胚」の一致で同一作家と確定）
- ユーザーが謎音研究所「謎音－忘却の川－」・花形槙「エルゴノミクス胚」を採用し、直接カレンダーに登録した。舞台芸術祭「秋の隕石2026東京」自体は会期が長い（10/9〜11/3）ため、フェスそのものはカレンダー登録の対象外とする方針が確定（個別演目のみ登録する）
- 追加の教訓（2026-09-16 続き）: **戯曲を追う軸を追加**。ユーザーから「つかこうへい、特に『熱海殺人事件』シリーズ」「唐十郎戯曲」は劇団を問わず好むとの指摘。events.jsonを確認すると「熱海殺人事件」は春匠／青春の会／劇団エドアブラ／ゴツプロ！／9PROJECT／大旅軍団という異なる6劇団で登録されており、劇団ローテーションでは検出できない軸だと確認できた。テイスト軸表に追加し、情報収集の系統に「戯曲軸検索（劇団名でなく作品名で探す）」を5番目として追加した
- 追加の教訓（2026-09-16 続きの続き）: **windowは上限ではなく実務的な既定値だと明確化**。ユーザーから「先の日程は週次で情報が変わりにくいという発想でこの期間設定にしているが、確実に検知できるなら会期がずっと先のものでもよい」との指摘。window（約6週間）は「直近ほど情報の出入りが多いので毎週律儀に見直す」ための運用上の既定値であって、候補を機械的に切り捨てる上限ではない。芸術祭ラインナップ・戯曲軸検索・公社流体力学のnote等で数ヶ月先の確実な情報を検知できた場合は、windowを無視してnextMonth/specialに載せてよいとWEEKLY_TASK.md・CLAUDE.mdに明記した

## 運用メモ（気づいた違和感）

週次タスク実行中に「仕組みが想定通り機能していない」と感じたことを日付付きで記録する場所（内容の当たり外れではなく、手順・データ・件数などの構造的な違和感が対象）。渡辺源四郎商店URL事故や虹屋敷の二重登録事故のような個別教訓は上のP1.x内に既に記録済み。ここは今後分の受け皿。同じ違和感が3回以上続いたら、次回実行時にチャットで明示的に見直しを提案する。

- 2026-08-28: 会場ローテーション5件（シアタートラム／三軒茶屋HEAVEN'S DOOR／世田谷パブリックシアター／シアター711／雑司ヶ谷鬼子母神堂）・劇団ローテーション印なし3件（のあんじー／果てとチーク／中野坂上デーモンズ）とも、窓の期間（約1ヶ月）内で嗜好に合う新規候補が0件だった。会場側は次回ラインナップが商業色の強い演目中心、劇団側は次回公演が未発表または窓の期間外。補完検索（排気口・深海洋燈等）でも同様に窓の外の情報しか見つからず、結果nextMonthは前週からの継続2件のみ。数週続くようなら、ローテーション対象の選び方（週替わりで偏る／小規模劇団は情報の出が遅い）を見直すか判断してほしい。
- 2026-09-11: 同様の事象が再発。会場ローテーション5件のうち新宿シアタートップスで新規候補2件（おぶちゃ／壱人前企画）を発見したが、いずれも作風を裏付ける情報がWeb検索で得られず「嗜好に合う」と言い切れる根拠が作れなかったため見送り、他4会場（オフ・オフ・シアター／ときわ座／吉祥寺シアター／浅草九劇）は該当なし。劇団ローテーション6件は登録済み3件を除く3件（糸あやつり人形一糸座／劇団桟敷童子／平原演劇祭）とも窓の期間内の次回公演情報が確認できず。結果nextMonthは前週からの継続4件のみ（1件は上演終了間近のため除外）。「ローテーション対象が商業色の強い会場に当たる週は空振りしやすい」「小規模劇団は作風を裏付ける二次情報が少なく、reasonを具体化できず見送りになりやすい」という2点が2回連続で発生。3回目が起きたら見直しを提案する。
- 2026-09-16: 3回目が発生。ユーザーが3件（ザジ・ズー／渡辺源四郎商店／ONEOR8）を却下した後、ローテーション以外の切り口（好きな劇団群の網羅チェック・窓を6週間に拡大した上での花園神社／駅前劇場／中野坂上デーモンズ等の再確認）まで広げて再探索したが、新規候補は0件だった（唐組「虹屋敷」は既登録、駅前劇場の新規演目は嗜好と離れる商業寄り、中野坂上デーモンズ・花園神社の追加情報なし）。nextMonthは一時空欄で確定。ルールに従い、この回の完了報告で明示的に見直しを提案する。
